// If you call "isNaN" on something, it checks to see if that
// thing is NOT a number:
// Another function with if/else:
var isEven = function(number) {
    if (number % 2 === 0) {
        return true;
    } else {
        return false;
    }
};



